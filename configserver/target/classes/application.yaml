spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/halitkalayci/turkcellms-configurations                                                                        #ConfigServerın konfigürasyonları nerden okuyacağını belirtiriz. Bu genelde github repositorsidir.

server:
  port: 8079
